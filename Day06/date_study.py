import datetime as z  #as 는 별명 이제 as 뒤에 쓰는 거라고 부를게

#random 랜덤한 수를 뽑는 라이브러리
#os(운영체제) 파일 생성, 삭제, 환경변수 조작
# csv 파일로 된 것을 조작


x = z.datetime.now()
print(x)
print(x.year)
print(x.month)
print(x.day)
print(x.hour)
print(x.second)

